%sql
CREATE OR REPLACE TEMPORARY VIEW tmp_rea
USING csv
OPTIONS (
  path 'dbfs:/FileStore/distinct_rea_vs_output.csv',
  header 'true'              -- el CSV tiene cabecera
);

SELECT
  NVL(GEOGRAPHICAL_BREAKDOWN, 'NULO') AS first_column,
  COUNT(*) AS cnt
FROM tmp_rea
GROUP BY first_column;
