import re
import msal
import requests

scope_name = "scpstresscrit102"
secret_key = "gscplglbsp4stressauth101"

raw = dbutils.secrets.get(scope_name, secret_key)
parts = raw.split("[###]")

print("Numero de partes:", len(parts))
print("Longitudes:", [len(p) for p in parts])

client_secret = parts[0]
client_id = parts[1]
tenant_id = "35595a02-4d6d-44ac-99e1-f9ab4cd872db"

authority = f"https://login.microsoftonline.com/{tenant_id}"

app = msal.ConfidentialClientApplication(
    client_id=client_id,
    client_credential=client_secret,
    authority=authority
)

result = app.acquire_token_for_client(
    scopes=["https://graph.microsoft.com/.default"]
)

print(result.keys())

if "access_token" in result:
    print("Token Graph obtenido")
else:
    print("Error al obtener token")
    print(result)
