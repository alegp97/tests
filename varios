 // 👇 “Pasa de loadJSon”: no ejecuta nada
    PowerMockito.doNothing().when(Configuration.class, "loadJSon");

    // Si getQuery usa loadJSon internamente, ahora no hará nada.
    // Ahora stub del resto que sí quieres:
    PowerMockito.mockStatic(VariableResolutionHandlerFactory.class);
    BDDMockito.given(VariableResolutionHandlerFactory.getVariableResolutionHandler())
        .willReturn(variablesResHandler);

    PowerMockito.when(variablesResHandler.translateQuery(session, execution, querycontent))
        .thenReturn(query);

    // 👇 IMPORTANTE: como has mockeado estáticos de Configuration,
    // tienes que decir qué debe devolver getQuery.
    PowerMockito.when(Configuration.getQuery(K.QUERY_ERR_DETAIL_VT, session, execution))
        .thenCallRealMethod();
