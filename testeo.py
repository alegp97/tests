Buen punto. El método sigue aceptando `location`, pero tras el cambio a `CREATE TABLE ... USING DELTA` ya no lo utiliza, así que el comportamiento deja de ser equivalente al anterior. Si los callers siguen esperando crear la tabla en una ruta concreta, esto introduce un cambio funcional: ahora se creará como managed table en la ubicación por defecto. Hay que decidir explícitamente si queremos seguir soportando `LOCATION` o eliminar el parámetro y adaptar los call sites al nuevo comportamiento.
