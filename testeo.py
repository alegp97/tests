Correcto, esto no es solo una mejora sino un fix funcional. El método sigue aceptando `location`, pero tras el cambio ya no la utiliza, así que los callers dejan de crear la tabla en la ruta esperada y pasan a obtener una managed table en la ubicación por defecto. Debemos reintroducir la cláusula LOCATION cuando `location` venga informada para preservar el comportamiento funcional.
