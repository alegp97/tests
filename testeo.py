Buen punto. Esto no es solo una mejora menor: `generateCreate` sigue recibiendo `location`, pero el SQL generado ya no la usa, así que cambia el comportamiento funcional del método. Antes el caller podía esperar una tabla asociada a una ruta concreta; ahora obtiene una managed Delta table en la ubicación por defecto. Si `location` sigue formando parte del contrato porque hay callers que dependen de esa ruta, debemos reintroducir `LOCATION` en el DDL. Si el cambio a managed tables es intencional, entonces habría que eliminar el parámetro y adaptar los call sites.
