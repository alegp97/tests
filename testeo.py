Buen punto. Tal como está, el SQL generado queda en `CREATE TABLE IF NOT EXISTS <table> USING DELTA` sin definición de columnas ni CTAS/LIKE, lo que puede ser inválido o crear una tabla sin esquema utilizable según el contexto. Si este método debe materializar la staging table completa, entonces necesitamos generar el DDL con columnas o derivarlo desde una fuente explícita.
